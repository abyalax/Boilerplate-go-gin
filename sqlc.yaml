version: "2"
sql:
  - schema: db/migration
    queries: db/query/users
    engine: postgresql
    gen:
      go:
        sql_package: "pgx/v5"
        package: "users"
        out: "src/modules/users"

  - schema: db/migration
    queries: db/query/auth
    engine: postgresql
    gen:
      go:
        sql_package: "pgx/v5"
        package: "auth"
        out: "src/modules/auth"
